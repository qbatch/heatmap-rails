require 'spec_helper'
RSpec.describe Heatmap do
  it "has a version number" do
    expect(Heatmap::VERSION).not_to be nil
  end
  it "has total click to be count" do
     expect(Heatmap::VERSION).not_to be nil
  end
end
